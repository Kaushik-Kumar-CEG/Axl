{
  "name": "@axl/extension-skills",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent Skills discovery and activation for Axl",
  "license": "Apache-2.0",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json --force",
    "test": "pnpm --filter @axl/protocol build && pnpm --filter @axl/kernel build && node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@axl/extension-api": "workspace:*",
    "@axl/kernel": "workspace:*",
    "@axl/protocol": "workspace:*",
    "yaml": "2.8.3"
  }
}
